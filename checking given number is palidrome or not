#include <stdio.h>
int n,rev,temp,a;
int main()
{
    printf("enter a number");
    scanf("%d",&n);
    a=n;
    while(n>0)
    {
        rev=n%10;
        temp=temp*10+rev;
        n=n/10;
        
    }
    printf("reverse of given number%d is %d",a,temp);
}
